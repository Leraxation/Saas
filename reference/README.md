# Reference photographs — not served

Source photos for the Higgsfield reveal pipeline, grouped by the upload part
they arrived in. They are deliberately **outside `public/`** so Next.js never
serves them: they inform generation prompts and nothing on the site displays
them.

Everything the site shows lives in `public/vehicles/part-N/` and is generated.
See `docs/higgsfield-pipeline.md`.
